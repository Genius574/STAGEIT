<h1>Items#index</h1>
<div class="container">
  <div class="cards">
    <% @items.each do |item| %>
    <%= link_to item_path(item) do %>
      <div class="card-category">
        <%= item.name %>
      </div>
    <% end %>
    <% end %>
  </div>
<%= link_to "create item", new_item_path %>
</div>
